local cloneref = cloneref or function(x) return x end

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local Camera = Workspace.CurrentCamera

local Drawing = (syn and syn.drawing) or (getrenv and getrenv().Drawing) or Drawing
local createDrawing = function(drawType, props)
    local obj = Drawing.new(drawType)
    if props then
        for k, v in pairs(props) do
            obj[k] = v
        end
    end
    return obj
end

local GLOBAL_FONT = "UI"
local GLOBAL_SIZE = 10

local function getScreenOffset(centerPos, size)
    local look = CFrame.new(centerPos, Camera.CFrame.Position)
    local halfW = size.X / 2
    local halfH = size.Y / 2
    local topRight, _ = Camera:WorldToViewportPoint((look * CFrame.new(halfW, halfH, 0)).Position)
    local bottomRight, _ = Camera:WorldToViewportPoint((look * CFrame.new(halfW, -halfH, 0)).Position)
    local vec2, onscreen = Camera:WorldToViewportPoint(centerPos)
    if not onscreen then return nil end
    local offsetX = math.max(topRight.X - vec2.X, bottomRight.X - vec2.X)
    local offsetY = math.max((vec2.Y - topRight.Y), (bottomRight.Y - vec2.Y))
    return Vector2.new(vec2.X, vec2.Y), Vector2.new(offsetX, offsetY)
end

local function getHealthColor(pct)
    return Color3.new(1,0,0):Lerp(Color3.new(0,1,0), pct)
end

local playerESP = {
    playerCache = {},
    drawingCache = {},
    childAddedConnections = {},
    childRemovedConnections = {},
}
playerESP.__index = playerESP

function playerESP.onChildAdded(fn) table.insert(playerESP.childAddedConnections, fn) end
function playerESP.onChildRemoved(fn) table.insert(playerESP.childRemovedConnections, fn) end

function playerESP.new(player)
    local self = setmetatable({
        player = player,
        connections = {},
        hidden = false,
        current = nil,
        drawings = nil,
        allDrawings = nil,
    }, playerESP)

    local cache = playerESP.drawingCache[1]
    if cache then
        table.remove(playerESP.drawingCache, 1)
        cache.name.Text = player.DisplayName
        self.allDrawings = cache.all
        self.drawings = cache
    else
        self:createWidget()
    end

    table.insert(self.connections, player.CharacterAdded:Connect(function(...)
        return self:characterAdded(...)
    end))
    table.insert(self.connections, player.CharacterRemoving:Connect(function(...)
        return self:characterRemoved(...)
    end))

    if player.Character then
        self:characterAdded(player.Character, true)
    end

    playerESP.playerCache[player] = self
    return self
end

function playerESP.remove(player)
    local cache = playerESP.playerCache[player]
    if not cache then return end
    playerESP.playerCache[player] = nil
    for _, conn in ipairs(cache.connections) do conn:Disconnect() end

    for _, obj in ipairs(cache.allDrawings) do obj.Visible = false end
    table.insert(playerESP.drawingCache, cache.drawings)
end

function playerESP:createWidget()

    local box = createDrawing("Square", {
        Visible = false,
        Filled = false,
        Thickness = 1,
        Color = Color3.new(1,1,1),
        ZIndex = 2,
    })
    local boxOutline = createDrawing("Square", {
        Visible = false,
        Filled = false,
        Thickness = 3,
        Color = Color3.new(0,0,0),
        ZIndex = 1,
    })

    local healthBg = createDrawing("Square", {
        Visible = false,
        Filled = true,
        Color = Color3.fromRGB(15,15,15),
        Transparency = 0.7,
        ZIndex = 1,
    })
    local healthFill = createDrawing("Square", {
        Visible = false,
        Filled = true,
        Color = Color3.fromRGB(80,230,100),
        ZIndex = 2,
    })
    local healthText = createDrawing("Text", {
        Visible = false,
        Center = false,
        Outline = true,
        OutlineColor = Color3.new(0,0,0),
        Color = Color3.new(1,1,1),
        Size = 8,
        Font = GLOBAL_FONT,
        ZIndex = 3,
        Text = "",
    })

    local name = createDrawing("Text", {
        Visible = false,
        Center = true,
        Outline = true,
        OutlineColor = Color3.new(0,0,0),
        Color = Color3.new(1,1,1),
        Size = GLOBAL_SIZE,
        Font = GLOBAL_FONT,
        ZIndex = 2,
        Text = self.player.DisplayName,
    })

    local weapon = createDrawing("Text", {
        Visible = false,
        Center = true,
        Outline = true,
        OutlineColor = Color3.new(0,0,0),
        Color = Color3.new(1,1,1),
        Size = GLOBAL_SIZE - 1,
        Font = GLOBAL_FONT,
        ZIndex = 2,
        Text = "None",
    })

    local distance = createDrawing("Text", {
        Visible = false,
        Center = true,
        Outline = true,
        OutlineColor = Color3.new(0,0,0),
        Color = Color3.new(1,1,1),
        Size = GLOBAL_SIZE - 2,
        Font = GLOBAL_FONT,
        ZIndex = 2,
        Text = "",
    })

    local drawings = {
        box = box,
        boxOutline = boxOutline,
        healthBg = healthBg,
        healthFill = healthFill,
        healthText = healthText,
        name = name,
        weapon = weapon,
        distance = distance,
        all = { box, boxOutline, healthBg, healthFill, healthText, name, weapon, distance },
    }
    self.drawings = drawings
    self.allDrawings = drawings.all
end

function playerESP:hideDrawings()
    if self.hidden then return end
    self.hidden = true
    for _, obj in ipairs(self.allDrawings) do obj.Visible = false end
end

function playerESP:characterAdded(character, firstTime)
    self.current = {
        character = character,
        active = false,
        humanoid = character:FindFirstChildOfClass("Humanoid"),
        rootPart = character:FindFirstChild("HumanoidRootPart"),
        health = 0,
        maxHealth = 0,
        healthPercentage = 1,
        weapon = nil,
    }

    table.insert(self.connections, character.ChildAdded:Connect(function(child)
        self:childAdded(child)
    end))
    table.insert(self.connections, character.ChildRemoved:Connect(function(child)
        self:childRemoved(child)
    end))

    if self.current.humanoid then
        self:setupHumanoid(self.current.humanoid, firstTime)
    end
    if self.current.rootPart then
        self.current.active = true
    end
end

function playerESP:characterRemoved(character)
    self.current = nil
    self:hideDrawings()
end

function playerESP:childAdded(child)
    if child:IsA("Humanoid") then
        self.current.humanoid = child
        self:setupHumanoid(child)
    end
    for _, fn in ipairs(playerESP.childAddedConnections) do
        fn(self, child)
    end
end

function playerESP:childRemoved(child)
    if child == self.current.humanoid then
        self.current.humanoid = nil
        self.current.active = false
        self:hideDrawings()
    elseif child == self.current.rootPart then
        self.current.rootPart = nil
        self.current.active = false
        self:hideDrawings()
    end
    for _, fn in ipairs(playerESP.childRemovedConnections) do
        fn(self, child)
    end
end

function playerESP:setupHumanoid(humanoid, firstTime)
    local function updateHealth()
        local health = humanoid.Health
        local maxHealth = humanoid.MaxHealth
        local pct = health / maxHealth
        self.current.health = health
        self.current.maxHealth = maxHealth
        self.current.healthPercentage = pct
        local color = getHealthColor(pct)
        self.drawings.healthFill.Color = color
        self.drawings.healthText.Color = color
        self.drawings.healthText.Text = tostring(math.floor(health))
    end
    updateHealth()
    table.insert(self.connections, humanoid:GetPropertyChangedSignal("Health"):Connect(updateHealth))
    if firstTime then
        for _, child in ipairs(self.current.character:GetChildren()) do
            self:childAdded(child)
        end
    end
end

function playerESP:loop(settings, distance)
    local current = self.current
    if not current or not current.active or not current.rootPart then
        self:hideDrawings()
        return
    end

    local hrp = current.rootPart
    local centerPos = hrp.Position
    local size = Vector3.new(4.5, 6.2, 4.5)

    local vec2, offset = getScreenOffset(centerPos, size)
    if not vec2 then
        self:hideDrawings()
        return
    end

    local box = self.drawings.box
    local outline = self.drawings.boxOutline
    if settings.box then
        box.Visible = true
        box.Position = vec2 - offset
        box.Size = offset * 2
        box.Color = Color3.new(1,1,1)

        outline.Visible = true
        outline.Position = vec2 - offset - Vector2.new(1,1)
        outline.Size = offset * 2 + Vector2.new(2,2)
        outline.Color = Color3.new(0,0,0)
    else
        box.Visible = false
        outline.Visible = false
    end

    if settings.healthbar then
        local pct = current.healthPercentage
        local barW = 2
        local barX = vec2.X - offset.X - barW - 3
        local barY = vec2.Y - offset.Y
        local barH = offset.Y * 2

        local bg = self.drawings.healthBg
        bg.Visible = true
        bg.Position = Vector2.new(barX, barY)
        bg.Size = Vector2.new(barW, barH)
        bg.Transparency = 0.7

        local fill = self.drawings.healthFill
        fill.Visible = true
        fill.Position = Vector2.new(barX, barY + barH * (1 - pct))
        fill.Size = Vector2.new(barW, barH * pct)
        fill.Color = getHealthColor(pct)

        if settings.healthText ~= false then
            local txt = self.drawings.healthText
            txt.Visible = true
            txt.Position = Vector2.new(barX - 2, barY + barH * (1 - pct))
            txt.Text = tostring(math.floor(current.health))
            txt.Color = fill.Color
            txt.Size = 8
        else
            self.drawings.healthText.Visible = false
        end
    else
        self.drawings.healthBg.Visible = false
        self.drawings.healthFill.Visible = false
        self.drawings.healthText.Visible = false
    end

    if settings.name then
        local name = self.drawings.name
        name.Visible = true
        name.Text = self.player.DisplayName
        name.Color = Color3.new(1,1,1)
        name.Size = GLOBAL_SIZE
        name.Position = Vector2.new(vec2.X, vec2.Y - offset.Y - name.Size - 2)
    else
        self.drawings.name.Visible = false
    end

    if settings.weapon then
        local weapon = self.drawings.weapon
        weapon.Visible = true

        local gunName = "None"
        local char = current.character
        if char then
            for _, child in ipairs(char:GetChildren()) do
                if child:IsA("Tool") then
                    gunName = child.Name
                    break
                end
            end
        end
        weapon.Text = gunName
        weapon.Color = Color3.new(1,1,1)
        weapon.Size = GLOBAL_SIZE - 1
        weapon.Position = Vector2.new(vec2.X, vec2.Y + offset.Y + 2)
    else
        self.drawings.weapon.Visible = false
    end

    if settings.distance then
        local dist = self.drawings.distance
        dist.Visible = true
        local mag = distance or (Camera.CFrame.Position - centerPos).Magnitude
        local unit = "s"
        local val = math.floor(mag)
        dist.Text = val .. unit
        dist.Color = Color3.new(1,1,1)
        dist.Size = GLOBAL_SIZE - 2
        local yOff = settings.weapon and 13 or 0
        dist.Position = Vector2.new(vec2.X, vec2.Y + offset.Y + 2 + yOff)
    else
        self.drawings.distance.Visible = false
    end
end

local entityESP = {
    entityCache = {},
    drawingCache = {},
}
entityESP.__index = entityESP

function entityESP.new(entity, settingName, name, color)
    local self = setmetatable({
        entity = entity,
        settingName = settingName,
        name = name or entity.Name,
        color = color or Color3.new(1,1,1),
        hidden = false,
        connections = {},
        drawings = nil,
        allDrawings = nil,
    }, entityESP)

    local cache = entityESP.drawingCache[1]
    if cache then
        table.remove(entityESP.drawingCache, 1)
        cache.name.Text = self.name
        cache.name.Color = self.color
        cache.box.Color = self.color
        cache.distance.Color = self.color
        self.allDrawings = cache.all
        self.drawings = cache
    else
        self:createWidget()
    end

    table.insert(self.connections, entity.AncestryChanged:Connect(function(child, parent)
        if child == entity and parent == nil then
            self:remove()
        end
    end))

    entityESP.entityCache[entity] = self
    return self
end

function entityESP:remove()
    entityESP.entityCache[self.entity] = nil
    self:hideDrawings()
    for _, conn in ipairs(self.connections) do conn:Disconnect() end
    table.insert(entityESP.drawingCache, self.drawings)
end

function entityESP:createWidget()
    local box = createDrawing("Square", {
        Visible = false,
        Filled = false,
        Thickness = 1,
        Color = self.color,
        ZIndex = 2,
    })
    local boxOutline = createDrawing("Square", {
        Visible = false,
        Filled = false,
        Thickness = 3,
        Color = Color3.new(0,0,0),
        ZIndex = 1,
    })
    local name = createDrawing("Text", {
        Visible = false,
        Center = true,
        Outline = true,
        OutlineColor = Color3.new(0,0,0),
        Color = self.color,
        Size = GLOBAL_SIZE,
        Font = GLOBAL_FONT,
        ZIndex = 2,
        Text = self.name,
    })
    local distance = createDrawing("Text", {
        Visible = false,
        Center = true,
        Outline = true,
        OutlineColor = Color3.new(0,0,0),
        Color = self.color,
        Size = GLOBAL_SIZE - 2,
        Font = GLOBAL_FONT,
        ZIndex = 2,
    })
    local drawings = {
        box = box,
        boxOutline = boxOutline,
        name = name,
        distance = distance,
        all = { box, boxOutline, name, distance },
    }
    self.drawings = drawings
    self.allDrawings = drawings.all
end

function entityESP:hideDrawings()
    if self.hidden then return end
    self.hidden = true
    for _, obj in ipairs(self.allDrawings) do obj.Visible = false end
end

function entityESP:loop(settings, distance)
    local entity = self.entity
    if not entity or not entity.Parent then
        self:hideDrawings()
        return
    end

    local cframe, size = entity:GetBoundingBox()
    if not cframe then
        self:hideDrawings()
        return
    end

    local vec2, offset = getScreenOffset(cframe.Position, size)
    if not vec2 then
        self:hideDrawings()
        return
    end

    if settings.box then
        self.drawings.box.Visible = true
        self.drawings.box.Position = vec2 - offset
        self.drawings.box.Size = offset * 2
        self.drawings.box.Color = self.color

        self.drawings.boxOutline.Visible = true
        self.drawings.boxOutline.Position = vec2 - offset - Vector2.new(1,1)
        self.drawings.boxOutline.Size = offset * 2 + Vector2.new(2,2)
        self.drawings.boxOutline.Color = Color3.new(0,0,0)
    else
        self.drawings.box.Visible = false
        self.drawings.boxOutline.Visible = false
    end

    if settings.name then
        local name = self.drawings.name
        name.Visible = true
        name.Text = self.name
        name.Color = self.color
        name.Size = GLOBAL_SIZE
        name.Position = Vector2.new(vec2.X, vec2.Y - offset.Y - name.Size - 2)
    else
        self.drawings.name.Visible = false
    end

    if settings.distance then
        local dist = self.drawings.distance
        dist.Visible = true
        local mag = distance or (Camera.CFrame.Position - cframe.Position).Magnitude
        dist.Text = math.floor(mag) .. "s"
        dist.Color = self.color
        dist.Size = GLOBAL_SIZE - 2
        dist.Position = Vector2.new(vec2.X, vec2.Y + offset.Y + 2)
    else
        self.drawings.distance.Visible = false
    end
end

local npcESP = {
    npcCache = {},
    drawingCache = {},
}
npcESP.__index = npcESP

function npcESP.new(entity, settingName, name, color, ignoreR15)
    local self = setmetatable({
        entity = entity,
        settingName = settingName,
        name = name or entity.Name,
        color = color or Color3.new(1,1,1),
        ignoreR15 = ignoreR15 or false,
        hidden = false,
        connections = {},
        humanoid = nil,
        health = 0,
        maxHealth = 0,
        healthPercentage = 1,
        drawings = nil,
        allDrawings = nil,
    }, npcESP)

    local cache = npcESP.drawingCache[1]
    if cache then
        table.remove(npcESP.drawingCache, 1)
        cache.name.Text = self.name
        cache.name.Color = self.color
        cache.box.Color = self.color
        cache.distance.Color = self.color
        self.allDrawings = cache.all
        self.drawings = cache
    else
        self:createWidget()
    end

    local humanoid = entity:FindFirstChildOfClass("Humanoid")
    if humanoid and humanoid.Health > 0 then
        self.humanoid = humanoid
        table.insert(self.connections, entity.AncestryChanged:Connect(function(child, parent)
            if child == entity and parent == nil then
                self:remove()
            end
        end))
        table.insert(self.connections, humanoid:GetPropertyChangedSignal("Health"):Connect(function()
            if humanoid.Health <= 0 then
                self:remove()
            else
                self:updateHealth(humanoid)
            end
        end))
        self:updateHealth(humanoid)
        npcESP.npcCache[entity] = self
        return
    end

    local conn
    conn = entity.ChildAdded:Connect(function(child)
        if child:IsA("Humanoid") then
            conn:Disconnect()
            self.humanoid = child
            table.insert(self.connections, entity.AncestryChanged:Connect(function(ch, parent)
                if ch == entity and parent == nil then
                    self:remove()
                end
            end))
            table.insert(self.connections, child:GetPropertyChangedSignal("Health"):Connect(function()
                if child.Health <= 0 then
                    self:remove()
                else
                    self:updateHealth(child)
                end
            end))
            self:updateHealth(child)
            npcESP.npcCache[entity] = self
        end
    end)
    table.insert(self.connections, conn)
end

function npcESP:remove()
    npcESP.npcCache[self.entity] = nil
    self:hideDrawings()
    for _, conn in ipairs(self.connections) do conn:Disconnect() end
    table.insert(npcESP.drawingCache, self.drawings)
end

function npcESP:createWidget()
    local box = createDrawing("Square", {
        Visible = false,
        Filled = false,
        Thickness = 1,
        Color = self.color,
        ZIndex = 2,
    })
    local boxOutline = createDrawing("Square", {
        Visible = false,
        Filled = false,
        Thickness = 3,
        Color = Color3.new(0,0,0),
        ZIndex = 1,
    })
    local healthBg = createDrawing("Square", {
        Visible = false,
        Filled = true,
        Color = Color3.fromRGB(15,15,15),
        Transparency = 0.7,
        ZIndex = 1,
    })
    local healthFill = createDrawing("Square", {
        Visible = false,
        Filled = true,
        Color = Color3.fromRGB(80,230,100),
        ZIndex = 2,
    })
    local healthText = createDrawing("Text", {
        Visible = false,
        Center = false,
        Outline = true,
        OutlineColor = Color3.new(0,0,0),
        Color = Color3.new(1,1,1),
        Size = 8,
        Font = GLOBAL_FONT,
        ZIndex = 3,
        Text = "",
    })
    local name = createDrawing("Text", {
        Visible = false,
        Center = true,
        Outline = true,
        OutlineColor = Color3.new(0,0,0),
        Color = self.color,
        Size = GLOBAL_SIZE,
        Font = GLOBAL_FONT,
        ZIndex = 2,
        Text = self.name,
    })
    local distance = createDrawing("Text", {
        Visible = false,
        Center = true,
        Outline = true,
        OutlineColor = Color3.new(0,0,0),
        Color = self.color,
        Size = GLOBAL_SIZE - 2,
        Font = GLOBAL_FONT,
        ZIndex = 2,
    })
    local drawings = {
        box = box,
        boxOutline = boxOutline,
        healthBg = healthBg,
        healthFill = healthFill,
        healthText = healthText,
        name = name,
        distance = distance,
        all = { box, boxOutline, healthBg, healthFill, healthText, name, distance },
    }
    self.drawings = drawings
    self.allDrawings = drawings.all
end

function npcESP:updateHealth(humanoid)
    local health = humanoid.Health
    local maxHealth = humanoid.MaxHealth
    local pct = health / maxHealth
    self.health = health
    self.maxHealth = maxHealth
    self.healthPercentage = pct
    local color = getHealthColor(pct)
    self.drawings.healthFill.Color = color
    self.drawings.healthText.Color = color
    self.drawings.healthText.Text = tostring(math.floor(health))
end

function npcESP:hideDrawings()
    if self.hidden then return end
    self.hidden = true
    for _, obj in ipairs(self.allDrawings) do obj.Visible = false end
end

function npcESP:loop(settings, distance)
    local entity = self.entity
    if not entity or not entity.Parent then
        self:hideDrawings()
        return
    end

    local useR15 = self.humanoid and not self.ignoreR15
    local cframe, size
    if useR15 then
        local hrp = entity:FindFirstChild("HumanoidRootPart")
        if hrp then
            cframe = CFrame.new(hrp.Position)
            size = Vector3.new(4.5, 6.2, 4.5)
        else
            cframe, size = entity:GetBoundingBox()
        end
    else
        cframe, size = entity:GetBoundingBox()
    end

    if not cframe then
        self:hideDrawings()
        return
    end

    local vec2, offset = getScreenOffset(cframe.Position, size)
    if not vec2 then
        self:hideDrawings()
        return
    end

    if settings.box then
        self.drawings.box.Visible = true
        self.drawings.box.Position = vec2 - offset
        self.drawings.box.Size = offset * 2
        self.drawings.box.Color = self.color

        self.drawings.boxOutline.Visible = true
        self.drawings.boxOutline.Position = vec2 - offset - Vector2.new(1,1)
        self.drawings.boxOutline.Size = offset * 2 + Vector2.new(2,2)
        self.drawings.boxOutline.Color = Color3.new(0,0,0)
    else
        self.drawings.box.Visible = false
        self.drawings.boxOutline.Visible = false
    end

    if settings.healthbar and self.humanoid then
        local pct = self.healthPercentage
        local barW = 2
        local barX = vec2.X - offset.X - barW - 3
        local barY = vec2.Y - offset.Y
        local barH = offset.Y * 2

        self.drawings.healthBg.Visible = true
        self.drawings.healthBg.Position = Vector2.new(barX, barY)
        self.drawings.healthBg.Size = Vector2.new(barW, barH)
        self.drawings.healthBg.Transparency = 0.7

        self.drawings.healthFill.Visible = true
        self.drawings.healthFill.Position = Vector2.new(barX, barY + barH * (1 - pct))
        self.drawings.healthFill.Size = Vector2.new(barW, barH * pct)
        self.drawings.healthFill.Color = getHealthColor(pct)

        if settings.healthText ~= false then
            local txt = self.drawings.healthText
            txt.Visible = true
            txt.Position = Vector2.new(barX - 2, barY + barH * (1 - pct))
            txt.Text = tostring(math.floor(self.health))
            txt.Color = self.drawings.healthFill.Color
            txt.Size = 8
        else
            self.drawings.healthText.Visible = false
        end
    else
        self.drawings.healthBg.Visible = false
        self.drawings.healthFill.Visible = false
        self.drawings.healthText.Visible = false
    end

    if settings.name then
        local name = self.drawings.name
        name.Visible = true
        name.Text = self.name
        name.Color = self.color
        name.Size = GLOBAL_SIZE
        name.Position = Vector2.new(vec2.X, vec2.Y - offset.Y - name.Size - 2)
    else
        self.drawings.name.Visible = false
    end

    if settings.distance then
        local dist = self.drawings.distance
        dist.Visible = true
        local mag = distance or (Camera.CFrame.Position - cframe.Position).Magnitude
        dist.Text = math.floor(mag) .. "s"
        dist.Color = self.color
        dist.Size = GLOBAL_SIZE - 2
        dist.Position = Vector2.new(vec2.X, vec2.Y + offset.Y + 2)
    else
        self.drawings.distance.Visible = false
    end
end

local espLibrary = {
    playerESP = playerESP,
    entityESP = entityESP,
    npcESP = npcESP,
}

return espLibrary
